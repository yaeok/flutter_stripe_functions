class Customer {
  uid: string
  username: string
  email: string

  constructor(uid: string, username: string, email: string) {
    this.uid = uid
    this.username = username
    this.email = email
  }
}
